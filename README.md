# BIPA Loyalty Analytics

Synthetic CRVM/loyalty analytics prototype.

## Run
```bash
pip install -r requirements.txt
streamlit run app.py
```

The app includes flexible CSV column mapping. Required fields: customer ID, transaction date, net sales. Optional fields add margin, promotion, points and category analysis.

Synthetic data is for demonstration only.
